# RemeMate Backlog

> 本文件只保留未完成或延期事项的简要记录。已完成工作从这里移除；历史过程见
> [`PROGRESS.md`](./PROGRESS.md)，当前快照见 [`HANDOFF.md`](./HANDOFF.md)。不在本文件展开详细实现方案。

## 当前优先级

- **Review Story 多语言稳定性二次优化**（P1）：中文、法文真实 provider 测试仍有 `invalid_schema`，韩文存在偶发波动；补回归验证后重跑六语种 staging smoke。
- **公开内容与 SEO 发布**（P1）：Q&A、Blog、SEO 页面已在 staging 验证，尚未合并或部署生产。
- **SEO1.1 图片与内容补充**（P2）：方案已确认，等待发布窗口。
- **开放注册**（P2）：方案已确认，尚未发布。

## 发布前基础设施与安全

- **Token quota 并发一致性**（P1）：并发请求下的额度扣减仍需补强并验证。
- **SQLAlchemy metadata 漂移**（P1）：模型与迁移的一致性检查仍需补齐。
- **CI 自动迁移检查**（P1）：将迁移检查纳入持续集成。
- **生产备份自动化**（P1）：补齐可恢复的定期备份与验证。
- **Bitwarden 迁移评估**（P1）：评估敏感配置迁移和轮换安排。
- **登录 IP 限流**（P1）：补充登录接口的异常流量防护。
- **数据库 CHECK 约束**（P2）：补齐关键字段约束并完成兼容性验证。

## 产品与体验延期项

- **SessionPad 体验优化**（P2）：根据实际使用反馈继续收敛交互。
- **SessionPad 重复发送策略**（P2）：明确重复提交的产品规则。
- **Landing 窄屏换行**（P2）：修正窄屏下的标题布局。
- **CSV 星标字段**（P2）：补充导入导出中的星标支持。
- **移动端阅读工具栏**（P2）：处理工具栏与导航的遮挡问题。
- **全局忽略词**（P2）：评估跨用户、跨词条的忽略规则。

## 工程收敛项

- **Provisioning 引擎复用**（P2）：评估现有流程的复用边界。
- **健康检查与 RLS**（P2）：确认健康检查路径的权限隔离。
- **RLS 用户重置例外**（P2）：收敛高权限重置路径。
- **Gunicorn 超时与 SSE**（P2）：验证长请求和流式响应的生产配置。
- **隐私安全观测**（P2）：建立脱敏的 AI 失败观测方式。

## 记录规则

- 闭测期间的崩溃、数据丢失、认证/RLS、安全、核心流程和部署故障优先修复。
- 低风险体验反馈只记录简短背景、优先级和触发条件；开始实施前再单独形成短计划。
- 生产部署、迁移、密钥或数据相关操作必须获得明确批准。
