# RemeMate Backlog

> 对齐代码 `0fc53f4`（2026-09-05）。当前状态与验证边界见 [HANDOFF.md](./HANDOFF.md)。新功能先确认短计划，不把建议当作批准。

## 已批准顺序

仓库修剪 → D01 最小测试/发布收口 → D02 私有学习闭环观察 → 按使用情况选择 D03/D04。详见 [决策票](./plans/2026-09-05-product-decision-tickets.md)。修剪已提交 `12ad2e5`；[D01](./plans/2026-09-05-d01-test-release-guards.md) 已实现并验证、待提交决策。下一步先确认 D02 短计划，不直接开做。

## 优先：测试与安全遗留

- D01 已收口原 8 个失败：认证 3 个为 SQL `now()` 测试夹具时区问题，Receipt 2 个在隔离环境直接通过，SEO 3 个为正式内容已 indexable 后的旧预期；全量 902 passed。
- 确认新云机测试角色凭据重设后的连接恢复；staging 凭据曾进入错误输出，轮换与共享配置修改需另获批准。详细边界见 HANDOFF。
- Practice 崩溃、错题选择、状态丢失、RLS/隔离与额度误扣继续立即处理。

## P1：发布可靠性

- **D01（已实现验证，待提交）**：配置防误连/脱敏、一次性 PostgreSQL 运行器、时区回归、迁移往返/metadata、最小权限 CI 已完成；详细证据见 D01 计划与 HANDOFF。
- **可恢复备份**：自动化生产备份，并定期隔离恢复验证；生产操作另行批准。
- **认证防护**：补齐登录 IP 异常流量限制，保持认证 token 与敏感字段脱敏。

## 按反馈决定

- **Practice 音质**：声音选择/语速记忆已本地合并并验收；是否引入云端 TTS 需真实反馈与新范围确认。
- **更多语言**：当前代码支持法/日/中文；其他语种逐项确认，不再按“仅法语”规划。
- **AI 额度**：失败不扣完成额度、内部重试成本分离、并发及脱敏已有实现；请求级幂等若有新需求单独定义（重复用户提交当前算新完成）。

## 明确后排

- Review Story 多语言稳定性：已知 `invalid_schema` 问题保留，不自动启动。

## 发布待批准

- `0fc53f4` 语音控制已合并本地 master，尚未 push 或部署生产。
- 生产部署、迁移、密钥与生产数据操作必须明确批准。
